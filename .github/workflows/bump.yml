name: bump

on: [push]

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v1
    - uses: tool3/bump@master
    - runs: 'npm -v'
      with:
        github_token: ${{ secrets.GP_TOKEN }}
        user: 'tal hayut'
        email: 'tal.hayut.dev@gmail.com'
