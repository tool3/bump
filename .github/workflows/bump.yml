name: bump

on: [push]

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
    - uses: tool3/bump@master
      with:
        github_token: ${{ secrets.GP_TOKEN }}
        user: 'T hayut'
        email: 'tal.dev@gmail.com'
